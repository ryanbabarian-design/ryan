# V2.3.12 적용부서 드롭다운 최신 조직도 동기화

## 수정 내용
- 전자결재 설정의 `적용부서` 목록을 현재 활성 직원명단(`employees.active=true`)의 부서만 사용하도록 수정.
- 과거 제안서에 남아 있는 예전 부서명은 더 이상 결재자 적용부서 드롭다운에 섞이지 않음.
- 같은 부서명은 1번만 표시하며 공백을 제거하고 가나다순 정렬.
- DB/SQL 변경 없음. 이메일 Cron, 결재선, 기존 결재자 배정 데이터는 변경하지 않음.

## GitHub 업로드
- `index.html`
- `js/app.js`

`js/config.js`는 건드리지 마세요.
